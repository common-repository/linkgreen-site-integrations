import '../scss/app.scss';

/*jquery(document).ready(function($) {
  $.post(ajaxurl, {
    action: 'linkgreen_sites_refresh_api'
  }, function(response) {
    console.log('AJAX complete');
  });
}); */
